apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-dashboard.fullname" . }}
  labels:
    {{- include "app-dashboard.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "app-dashboard.selectorLabels" . | nindent 4 }}
